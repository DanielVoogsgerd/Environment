cd ~
if [ ! -d "$HOME/Environment/.git/" ]; then
	git clone https://github.com/Fryslan123/dotfiles.git Environment
else
	cd Environment
fi

cd Environment
git submodule init
git submodule update
git pull

git config --global user.name "Fryslan123"
git config --global user.email "sibrenvas@gmail.com"

python link.py Server

curl https://raw.github.com/creationix/nvm/master/install.sh | sh
\curl -L https://get.rvm.io | bash -s stable

rvm install 1.9
rvm use 1.9

gem install bundler

vim +BundleInstall