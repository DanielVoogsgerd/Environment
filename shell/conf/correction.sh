#!/usr/bin/zsh
# I have lot's of bad habbits to get rid of. To get rid of them I disable them using these aliases.
if [ $ENV_CORRECTION_MODE ]; then
	alias vim='echo "Please stop using vim, use edit instead"'
fi
